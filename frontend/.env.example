# Base URL of the deployed backend API.
# Set this to your Render/Railway backend URL for production.
# Defaults to http://localhost:8000 if not set.
VITE_API_BASE_URL=https://your-backend.onrender.com
